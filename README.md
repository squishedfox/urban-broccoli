# urban-broccoli

Personal Resume builder from markdown

## Getting started

1. Make sure to use node version 22.12.0 for this project. Node 24 is not supported
2. Install dependencies with `npm install`
3. Start the app with `npm start`
